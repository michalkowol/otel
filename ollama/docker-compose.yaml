services:
  ollama:
    image: ollama/ollama:0.21.2
    restart: unless-stopped
    volumes:
      - ollama_storage:/home/node/.olla
    networks:
      - dokploy-network

volumes:
  ollama_storage:

networks:
  dokploy-network:
    external: true
